# TicAmazon — Tabla de resultados y ejecución de pruebas

| Tipo | Prueba | ¿Qué se comprueba? | ¿Cómo se ejecuta? | Resultado |
|---|---|---|---|---|
| **Preparación** | Dependencias | Tener las herramientas necesarias para compilar y probar el proyecto. | `sudo dnf install gcc-c++ make openssl-devel python3 curl wget` | Preparación |
| **Automática** | **Suite completa** | Compila servidor y simulación, ejecuta FileSystem, HTTP y simulación. Los resultados se guardan en un registro. | Desde `Entregable2`: `bash tests/run_tests.sh`<br>Consultar resultados: `cat tests/results/execution.log` | **Aprobada** en la ejecución registrada; revisar que no haya `skipped`. |
| **FileSystem (C++)** | Creación, inserción y consultas | Crear una bodega; buscar una inexistente; insertar seis productos, listarlos y comprobar que se generan más bloques al necesitar espacio. | `bash tests/run_tests.sh` (incluye `filesystem_test.cc`). | **Aprobado** |
| **FileSystem (C++)** | Actualizaciones y filtros | Cambiar la cantidad y el precio de productos y buscar los productos por categoría. | `bash tests/run_tests.sh` (mismo programa C++). | **Aprobado** |
| **FileSystem (C++)** | Persistencia y eliminación | Recrear el objeto `FileSystem` y comprobar que se conservan los productos y cambios; eliminar un producto y verificar que la eliminación permanece. | `bash tests/run_tests.sh` (mismo programa C++). | **Aprobado** |
| **HTTP (automática)** | Categorías en JSON y HTML | `GET /categories` devuelve las categorías y el formato solicitado. | `python3 -m unittest discover -s tests -p 'test_http.py' -v`<br>**Si no detecta la IP:** `TIC_HOST=192.168.1.30 bash tests/run_tests.sh` (**sustituir** `192.168.1.30` por la IP propia; ejecuta toda la suite). | **2 aprobadas** |
| **HTTP (automática)** | Productos y errores de consulta | Consultar una categoría existente; solicitar productos sin el filtro obligatorio (`400`) y una ruta inexistente (`404`). | Mismo comando de HTTP de la fila anterior. | **3 aprobadas** |
| **HTTP (automática)** | Compras y validación | Rechazar datos de compra incorrectos (`400`); descontar existencias correctamente y rechazar compras sin stock suficiente (`409`). | Mismo comando de HTTP de la fila anterior. | **2 aprobadas** |
| **HTTP (automática)** | Compras simultáneas | Comprobar que dos compras concurrentes no vendan más unidades de las disponibles. | Mismo comando de HTTP de la fila anterior. | **1 aprobada** |
| **Simulación (automática)** | Consulta y cierre | La simulación responde a una consulta y termina correctamente. | `python3 -m unittest discover -s tests -p 'test_simulation.py' -v` (compilar primero con `make -C src/Simulacion` si no se ejecutó la suite). | **Aprobada** |
| **Preparación manual** | Iniciar servidor | Levantar el servidor de productos para las pruebas desde otros equipos. | Desde `Entregable2`: `make -C src/ServidorProductos`<br>`cd src/ServidorProductos && ./servidor`<br>Elegir servidor de productos (`1`), puerto de ejemplo (`5555`) y sin SSL (`2`). | Preparación |
| **Manual** | Acceso remoto | Acceso HTTP desde otra computadora de la misma red sin usar `localhost`. | Desde otro equipo: `curl -i http://IP:5555/categories` y `wget -qO- http://IP:5555/categories`. Sustituir `IP` por la IP real del servidor. | **Resultado esperado**, según lo reportado. |
| **Manual** | Navegador y celular | Visualizar las categorías desde otro dispositivo y utilizar la página del proyecto. | Abrir `http://IP:5555/categories` desde un navegador. Para el frontend, desde `Entregable2` iniciar `python3 -m http.server 8000 --bind 0.0.0.0` y abrir `http://IP:8000/menosxmas-frontend.html`. | **Resultado esperado**, según lo reportado. |
| **Manual** | Carrito y proforma | Agregar productos y comprobar cantidades, subtotales, impuesto y total. | Desde el frontend, agregar al menos dos productos y solicitar la proforma con inventario de prueba. | **Resultado esperado**, según lo reportado. |
| **Manual** | Fallo en compra múltiple | Observar qué pasa con los productos anteriores cuando una compra posterior falla por falta de stock. | Preparar un carrito de dos productos, dejar el segundo sin stock suficiente y registrar el estado de ambos después del intento. | **Comportamiento observado según lo esperado para el escenario probado**; no demuestra reversión automática. |
| **Manual** | Persistencia tras reinicio | Confirmar que el cambio de existencias se conserva cuando se reinicia el servidor. | Consultar stock, hacer una compra de prueba, detener y reiniciar `./servidor`, y volver a consultar el mismo producto. | **Resultado esperado**, según lo reportado. |
| **Manual** | Simulación y bitácora | Revisar las consultas del menú, los mensajes del protocolo y el cierre. | Desde `Entregable2`: `cd src/Simulacion && make && ./simulación almacen 1 2`.<br>Probar el menú y revisar `cat bitacora.log` desde esa misma carpeta. | **Resultado esperado**, según lo reportado. |
| **Manual** | Entradas incorrectas | Verificar el manejo de solicitudes incompletas o parámetros inválidos sin interrumpir el servicio. | Enviar solicitudes inválidas al servidor de pruebas, registrar sus respuestas y repetir una consulta válida para comprobar que siga activo. | **Resultado esperado en los escenarios probados**, según lo reportado. |
| **Manual** | Packet Tracer | Comprobar la topología, las direcciones y la comunicación entre los equipos previstos. | Abrir el archivo `.pkt`, revisar direcciones y ejecutar `ping` entre los dispositivos correspondientes. | **Resultado esperado**, según lo reportado. |
| **Manual** | HTTPS | Comprobar que el servidor responde usando SSL. | Reiniciar `./servidor` con SSL (`1`) y ejecutar `curl -k -i https://IP:PUERTO/categories`. La opción `-k` omite la validación del certificado. | **Resultado esperado**, según lo reportado. |
| **Requisito aparte** | Formato de almacenamiento | Comprobar que se utiliza un único archivo de disco y que cada bloque **escrito** no supera los 256 bytes. La prueba funcional del FileSystem no verifica estos requisitos. | Tras ajustar o confirmar el diseño, revisar el archivo generado y medir el formato realmente escrito en disco. | **Pendiente de verificación arquitectónica** |
| **Requisito aparte** | Sincronización entre intermediarios | Comprobar altas, bajas, notificaciones y sincronización de bodegas entre islas. | Cuando esté integrado, iniciar varios intermediarios, registrar y retirar bodegas, y contrastar sus registros y bitácoras. | **Pendiente de integración y evidencia en la copia revisada** |
